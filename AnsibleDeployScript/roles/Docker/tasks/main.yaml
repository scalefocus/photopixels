---
- name: Ensure Docker is installed
  become: yes
  apt:
    name: docker.io
    state: present
    update_cache: yes

- name: Ensure Docker service is running
  become: yes
  service:
    name: docker
    state: started
    enabled: true